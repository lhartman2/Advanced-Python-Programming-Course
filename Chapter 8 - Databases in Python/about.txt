Now we are introducing the use of databases in python code. We use the simple SQLite framework and local database to practice SQL and the commands python uses. In the next class I introduce the use of ORM's with SQLAlchemy and the like. In the course I split this chapter up into two modules. Insert and Select in the first module then Updates and Deletes after. Otherwise a whole transition from files to databases is long.
